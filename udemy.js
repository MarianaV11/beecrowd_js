let x = 10;
x = X + 5;

console.log(X);

let y =  20;
y += 10;
/* isso é o que chamamos de atribuição aditiva,
logo ele apenas somará a var 20 com 10, mas se 
colocasemos y = 20;y = 10, nós estariamos substituindo
o seu valor.
*/ y+=10 == y = y+10;
let w = 'texto';
w += ', mais texto'
//output(texto, mais texto) concatenou as strings

let texto = 'Bom dia!\nTudoBem?';
// \n serve para quebrar a string executando ela abaixo.
// \t ele tem a função do 'tab'.
